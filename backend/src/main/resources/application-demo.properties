# Use together with the local profile. This profile is deliberately opt-in.
app.demo.enabled=true
spring.datasource.url=jdbc:h2:mem:retro-demo;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;DEFAULT_NULL_ORDERING=HIGH
spring.jpa.hibernate.ddl-auto=create-drop
